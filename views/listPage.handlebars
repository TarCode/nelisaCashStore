<div id="page-wrapper" >
    <div id="page-inner">
        <div class="row">
            <div class="col-md-12 col-sm-12 col-xs-12">
                <h3>Nelisa's Cash Store</h3>
                <p>Welcome to Nelisa's Cash Store Inventory System</p>
            </div>
        </div>
        <!-- /. ROW  -->
        <hr />

        <!-- /. ROW  -->
        <div class="row">

            <div class="col-md-6 col-sm-8 col-xs-8">
                <div class="panel panel-default">
                    <div class="panel-heading">
                        Name
                    </div>
                    <div class="panel-body">
                        {{#each item}}
                        <div>{{name}}</div>
                        {{/each}}
                    </div>
                </div>
            </div>

            <div class="col-md-6 col-sm-4 col-xs-4">
                <div class="panel panel-default">
                    <div class="panel-heading">
                        Value
                    </div>
                    <div class="panel-body">
                        {{#each item}}
                        <div>{{amt}}</div>
                        {{/each}}
                    </div>
                </div>
            </div>

        </div>
        <!-- /. ROW  -->
    </div>
</div>
</div>
